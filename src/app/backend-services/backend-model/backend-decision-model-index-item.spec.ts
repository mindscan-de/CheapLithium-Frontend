import { BackendDecisionModelIndexItem } from './backend-decision-model-index-item';

describe('BackendDecisionModelIndexItem', () => {
  it('should create an instance', () => {
    expect(new BackendDecisionModelIndexItem()).toBeTruthy();
  });
});

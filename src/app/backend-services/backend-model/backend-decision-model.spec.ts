import { BackendDecisionModel } from './backend-decision-model';

describe('BackendDecisionModel', () => {
  it('should create an instance', () => {
    expect(new BackendDecisionModel()).toBeTruthy();
  });
});

import { BackendDecisionThreadIndex } from './backend-decision-thread-index';

describe('BackendDecisionThreadIndex', () => {
  it('should create an instance', () => {
    expect(new BackendDecisionThreadIndex()).toBeTruthy();
  });
});

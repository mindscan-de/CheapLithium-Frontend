import { BackendDecisionModelDecisionNodeOutcome } from './backend-decision-model-decision-node-outcome';

describe('BackendDecisionModelDecisionNodeOutcome', () => {
  it('should create an instance', () => {
    expect(new BackendDecisionModelDecisionNodeOutcome()).toBeTruthy();
  });
});

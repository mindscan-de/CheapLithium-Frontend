import { BackendDecisionThreadReport } from './backend-decision-thread-report';

describe('BackendDecisionThreadReport', () => {
  it('should create an instance', () => {
    expect(new BackendDecisionThreadReport()).toBeTruthy();
  });
});

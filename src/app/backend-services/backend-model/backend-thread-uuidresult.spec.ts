import { BackendThreadUUIDResult } from './backend-thread-uuidresult';

describe('BackendThreadUUIDResult', () => {
  it('should create an instance', () => {
    expect(new BackendThreadUUIDResult()).toBeTruthy();
  });
});

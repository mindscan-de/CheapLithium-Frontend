import { BackendDecisionThread } from './backend-decision-thread';

describe('BackendDecisionThread', () => {
  it('should create an instance', () => {
    expect(new BackendDecisionThread()).toBeTruthy();
  });
});

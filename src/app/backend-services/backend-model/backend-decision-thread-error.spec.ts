import { BackendDecisionThreadError } from './backend-decision-thread-error';

describe('BackendDecisionThreadError', () => {
  it('should create an instance', () => {
    expect(new BackendDecisionThreadError()).toBeTruthy();
  });
});

import { BackendDecisionThreadInputUI } from './backend-decision-thread-input-ui';

describe('BackendDecisionThreadInputUI', () => {
  it('should create an instance', () => {
    expect(new BackendDecisionThreadInputUI()).toBeTruthy();
  });
});

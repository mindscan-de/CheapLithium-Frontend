import { BackendDecisionModelIndex } from './backend-decision-model-index';

describe('BackendDecisionModelIndex', () => {
  it('should create an instance', () => {
    expect(new BackendDecisionModelIndex()).toBeTruthy();
  });
});

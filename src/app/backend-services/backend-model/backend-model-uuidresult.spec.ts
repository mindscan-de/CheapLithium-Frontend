import { BackendModelUUIDResult } from './backend-model-uuidresult';

describe('BackendModelUUIDResult', () => {
  it('should create an instance', () => {
    expect(new BackendModelUUIDResult()).toBeTruthy();
  });
});
